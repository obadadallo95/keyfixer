# Experimental Desktop Python Script

This directory contains `keyfixer.py`, an experimental standalone Python prototype for KeyFixer desktop layout correction.

### Usage
```bash
python3 keyfixer.py
```
*Note: This script is an experimental proof-of-concept. The production KeyFixer engine resides in `src/core/keyboard/` (TypeScript).*
